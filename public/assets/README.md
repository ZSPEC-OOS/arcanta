# Arcanta placeholder assets

Binary image files were removed to keep this repository text-only in environments that do not support binary diffs.

The UI still references these expected asset paths and gracefully falls back to styled placeholders when files are missing:

- `/assets/arcanta-hero-emblem-primary.png`
- `/assets/arcanta-emblem-horizontal.png`
- `/assets/app-wolfkrow-dashboard.png`
- `/assets/app-bluswan-interface.png`
- `/assets/iphone-frame-transparent-screen.png`
- `/assets/icon-discover.png`
- `/assets/icon-design.png`

Drop your real files into `public/assets/` with these exact names.
